## 0.1.5

- Test Connection button on stdio servers — verifies reachability and shows elapsed time and tool count
- Delete Server option in the Edit Server view with confirmation dialog
- Server key is now auto-generated as a full slug from the display name; conflict detection prevents duplicate keys
- Edit Server form redesigned with labeled, bordered fields so editable areas are obvious
- Fixed About window appearing on launch
- Fixed Sparkle update relaunch after install

## 0.1.4

- New app icon with custom artwork — detailed at large sizes, clean silhouette at small sizes
- About panel with Evil Inc. cityscape background and frosted glass design
- Automatic in-app updates via Sparkle with styled release notes (dark mode aware)
- One-line install and uninstall scripts
